package leapstream.scoreboard.pylons.score.config;

import leapstream.scoreboard.core.config.exposed.ExposedConfig;
import leapstream.scoreboard.core.config.exposed.ConfigRegistry;

public class ScoreExposedConfig implements ExposedConfig {
    public void expose(ConfigRegistry registry) {
        // FIX CONFIG AAAAAAAA I am here. Wire in exposed config methods.

    }
}
